{
  "name": "gpui-ai-gallery-web",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node ../scripts/copy-icons.mjs",
    "dev": "vite",
    "prebuild": "node ../scripts/copy-icons.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test src/*.test.mjs"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
